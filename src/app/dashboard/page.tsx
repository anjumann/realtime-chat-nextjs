import Button from '@/components/ui/button'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return (
    <>
    <Button>
      Hello Bitch
    </Button>
    </>
    )
}

export default page 